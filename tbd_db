-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.activity (
  activity_id numeric NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  name text NOT NULL,
  description text NOT NULL,
  quorum bigint NOT NULL,
  CONSTRAINT activity_pkey PRIMARY KEY (activity_id)
);
CREATE TABLE public.activity_venue_map (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  activity_id bigint NOT NULL,
  venue_id bigint NOT NULL,
  start_time timestamp with time zone NOT NULL,
  end_time timestamp with time zone NOT NULL,
  is_active boolean NOT NULL DEFAULT false,
  is_public boolean NOT NULL DEFAULT false,
  max_people bigint NOT NULL DEFAULT '30'::bigint,
  parallel_slots bigint NOT NULL DEFAULT '1'::bigint,
  is_hosted boolean NOT NULL DEFAULT false,
  date date NOT NULL,
  is_ticketed boolean NOT NULL,
  ticket_price numeric NOT NULL,
  description text NOT NULL,
  img_url text,
  CONSTRAINT activity_venue_map_pkey PRIMARY KEY (id),
  CONSTRAINT activity_venue_map_activity_id_fkey FOREIGN KEY (activity_id) REFERENCES public.activity(activity_id),
  CONSTRAINT activity_venue_map_venue_id_fkey FOREIGN KEY (venue_id) REFERENCES public.venue(venue_id)
);
CREATE TABLE public.city (
  city_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  country text NOT NULL,
  name text NOT NULL,
  CONSTRAINT city_pkey PRIMARY KEY (city_id)
);
CREATE TABLE public.city_region (
  city_region_id numeric NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  city_id numeric NOT NULL,
  name text NOT NULL,
  CONSTRAINT city_region_pkey PRIMARY KEY (city_region_id)
);


CREATE TABLE public.locality (
  locality_id numeric NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  name text NOT NULL,
  pincode text NOT NULL,
  address text NOT NULL,
  latitude numeric NOT NULL,
  longitude numeric NOT NULL,
  city_region_id numeric NOT NULL,
  CONSTRAINT locality_pkey PRIMARY KEY (locality_id)
);
CREATE TABLE public.venue (
  venue_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  name text NOT NULL,
  latitude numeric NOT NULL,
  longitude numeric NOT NULL,
  google_maps_location text NOT NULL,
  directions_to_reach text,
  address text NOT NULL,
  is_public boolean DEFAULT false,
  is_active boolean DEFAULT false,
  is_verified boolean DEFAULT false,
  is_approved boolean DEFAULT false,
  price_point smallint,
  open_time text NOT NULL,
  close_time text NOT NULL,
  updated_at timestamp with time zone NOT NULL,
  locality_id bigint NOT NULL DEFAULT '-1'::bigint,
  CONSTRAINT venue_pkey PRIMARY KEY (venue_id),
  CONSTRAINT venue_details_locality_id_fkey FOREIGN KEY (locality_id) REFERENCES public.locality(locality_id)
);